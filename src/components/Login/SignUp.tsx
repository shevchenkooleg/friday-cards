import React from 'react';
import {Return} from "../Error404/Return";

export const SignUp = () => {
    return (
        <div>
            <div>Sign up</div>
            Something should be here, but now
            <Return/>
        </div>
    );
};